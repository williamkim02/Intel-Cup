# Dependencies for the SOH model benchmark (benchmark_models.py / benchmark_charge.py / export_models.py)
numpy
pandas
scipy
scikit-learn
matplotlib
joblib
torch
# To reproduce the sandbox run exactly (requires Python <= 3.11):
#   pip install numpy==1.26.4 torch==2.2.2
